# 層級式導航系統總覽

## 📋 概述

本文件整合了層級式導航系統的所有核心概念，包括 AIT* 全局規劃器、RL 局部控制器、動態重規劃、Frontier 探索、課程學習和安全過濾器。

---

## 🏗️ 系統架構

```
┌───────────────────────────────────────────────────────────────────────────┐
│                        層級式導航系統                                       │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌─────────────────┐        ┌─────────────────┐       ┌───────────────┐  │
│  │   AIT* 全域     │   路徑   │   Local Goal    │  目標  │    RL PPO     │  │
│  │   規劃器        │ ───────► │   提取器        │ ─────► │    控制器      │  │
│  │   (大腦)        │  1 Hz   │   (介面層)      │ 10 Hz │   (小腦)       │  │
│  └─────────────────┘        └─────────────────┘       └───────────────┘  │
│         │                            │                        │           │
│         ▼                            ▼                        ▼           │
│  [已知地圖]                   [局部目標點]              [動作輸出]        │
│  + 當前位置                    + 相對座標              v, ω              │
│  + 目標位置                                              │               │
│                                ┌────────────────────────┘               │
│                                ▼                                        │
│                        ┌───────────────┐                               │
│                        │   安全過濾器    │                               │
│                        │  (CBF/規則)   │                               │
│                        └───────────────┘                               │
│                                ▼                                        │
│                        ┌───────────────┐                               │
│                        │   機器人      │                               │
│                        │   (Charge)    │                               │
│                        └───────────────┘                               │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘
```

---

## ⚠️ 重要：Phase vs Stage 區分

本專案有**兩套獨立的訓練階段系統**：

### 1️⃣ 端到端 RL 訓練階段（Phase 0-3）

這是**當前主要使用**的訓練流程，註冊在 `__init__.py` 中：

| Phase | 環境 ID | 障礙物 | 目標距離 | 描述 |
|:----:|---------|:------:|:--------:|------|
| **0** | `Isaac-Navigation-Charge-Phase0` | 無 | 3-8m | 基礎運動學，無 LiDAR |
| **v0** | `Isaac-Navigation-Charge-v0` | 無（只有牆壁）| 2-15m（可變）| 長程導航，自適應課程 |
| **v1** | `Isaac-Navigation-Charge-v1` | 3 個靜態 | 3-8m | 靜態避障入門 |
| **v2** | `Isaac-Navigation-Charge-v2` | 5 個靜態 | 3-8m | 複雜靜態避障 |
| **v3** | `Isaac-Navigation-Charge-v3` | 無 | 2-15m（自適應）| 目標距離自適應課程 |

### 2️⃣ 層級式導航 Stage（AIT* + RL）

這是**實驗性功能**，整合 AIT* 全局規劃器：

| Stage | 障礙物密度 | 目標距離 | 描述 | 狀態 |
|:----:|:-----------:|:--------:|------|:----:|
| **1** | 0% | 5-15m | Goal-Oriented Following | 🔄 開發中 |
| **2** | 15% | 8-18m | Static Obstacle Navigation | 🔄 開發中 |
| **3** | 35% | 10-20m | Complex Topology | 🔄 開發中 |
| **4** | 40% + 動態 | 12-25m | Dynamic & Noisy Environment | 🔄 規劃中 |

**環境 ID**：
- `Isaac-Navigation-Charge-Hierarchical-v0`（無障礙）
- `Isaac-Navigation-Charge-Hierarchical-v1`（3 個障礙物）

---

## 📚 完整文件結構

```
charge_sb3/
├── hierarchical/
│   ├── __init__.py
│   ├── hierarchical_navigation_manager.py   # 主管理器 ⭐
│   ├── hierarchical_env_cfg.py            # 環境配置
│   └── frontier_exploration.py              # Frontier 探索
├── mdp/
│   ├── observations/
│   │   └── hierarchical_navigation.py      # 導航觀測 ⭐
│   ├── rewards/
│   │   └── hierarchical_rewards.py          # 導航獎勵 ⭐
│   ├── path_planner/
│   │   ├── aitstar_planner.py              # AIT* 規劃器 ⭐
│   │   ├── curriculum_manager.py            # 課程管理器 ⭐
│   │   ├── environment_map.py
│   │   └── path_smoother.py
│   └── events/
│       ├── curriculum.py                    # 自適應課程學習 ⭐
│       └── goal_distance_curriculum.py      # 目標距離課程 ⭐
└── md/
    ├── 層級式導航系統總覽.md                      # 本文件
    ├── 動態重規劃與Frontier探索.md          # 動態重規劃詳情
    ├── AITstar與RL課程學習架構.md            # 課程學習詳情
    └── AITstar路徑規劃器說明.md             # AIT* 規劃器詳情
```

---

## 🔗 相關文檔

- **動態重規劃詳情**: 詳見 `動態重規劃與Frontier探索.md`
- **AIT* 課程學習**: 詳見 `AITstar與RL課程學習架構.md`
- **AIT* 規劃器**: 詳見 `AITstar路徑規劃器說明.md`

---

## 🚀 快速開始

### 訓練命令（端到端 RL）

```bash
# Phase 0：基礎運動學
./isaaclab.sh -p scripts/reinforcement_learning/sb3/train_charge.py \
    --task Isaac-Navigation-Charge-Phase0 \
    --num_envs 256 --headless

# v1：3 個靜態障礙物
./isaaclab.sh -p scripts/reinforcement_learning/rsl_rl/train.py \
    --task Isaac-Navigation-Charge-v1 \
    --num_envs 4096

# v3：長程導航（自適應課程）
./isaaclab.sh -p scripts/reinforcement_learning/sb3/train_charge.py \
    --task Isaac-Navigation-Charge-SB3-v3 \
    --num_envs 256 --headless

# 層級式導航（實驗性）
./isaaclab.sh -p scripts/reinforcement_learning/rsl_rl/train.py \
    --task Isaac-Navigation-Charge-Hierarchical-v0
```

### 核心 API（層級式導航）

```python
from isaaclab_tasks.manager_based.locomotion.velocity.config.charge_sb3.hierarchical import (
    HierarchicalNavigationManager,
)

manager = HierarchicalNavigationManager(
    env=env,
    lookahead_distance=2.0,
    replan_interval=2.0,
    enable_visualization=True,
)

# 更新導航系統
debug_info = manager.update(dt=0.02)
```

---

## 📊 關鍵參數

### 端到端 RL 觀測空間（統一 131 維）

**設計原則**：所有 Phase 統一 131 維，沒有障礙物的 Phase 使用 padding 填充，確保權重可遷移。

```python
observation = [
    lidar_scan,           # [72]  LiDAR 掃描（360°/5°=72 條射線，歸一化）
    speed,                # [2]   底盤線速度 (vx, vy)
    goal_position,        # [2]   目標相對位置（機器人座標系）
    goal_distance,        # [1]   到目標的歐式距離
    time_remaining,       # [1]   剩餘時間比例
    alive_flag,           # [1]   存活標誌
    actions,              # [2]   上一步動作
    obstacles,            # [50]  障礙物資訊（10×5）
]  # 總計：131 維（所有 Phase 統一）
```

### 各 Phase 障礙物 Padding 策略

| Phase | 實際障礙物 | obstacles[50] 內容 |
|:-----:|:----------:|-------------------|
| **0** | 0 個 | 全為 0（padding）|
| **1** | 3 個 | 前 15 維實值，後 35 維 0 |
| **2** | 5 個 | 前 25 維實值，後 25 維 0 |
| **3** | 0 個 | 全為 0（padding）|

### 層級式導航觀測空間（AIT* + RL）

```python
observation = [
    lidar_scan,          # [72] LiDAR 掃描（360°/5°=72 條射線）
    distance,             # [1]  到局部目標的距離（歸一化）
    heading_error,        # [1]  航向誤差（弧度）
    forward,              # [1]  前向分量（歸一化）
    lateral,              # [1]  側向分量（歸一化）
    linear_vel,           # [1]  線速度（歸一化）
    angular_vel,          # [1]  角速度（歸一化）
]
```

### 獎勵函數（Phase 3）

```python
R_total = (
    500.0 * R_reaching_goal      # 抵達目標（決定性獎勵）
    + 3.0 * R_velocity_to_goal   # 朝目標速度
    + 5.0 * R_progress           # 距離進步
    - 200.0 * P_tipped_over      # 翻車懲罰
    - 0.05 * P_time_out          # 超時懲罰
)
```

---

## 🎯 自適應課程學習機制（實現於 mdp/events/curriculum.py）

### 升級/降級條件

```python
# 升級條件（增加難度）
if avg_success_rate >= 0.8 and avg_collision_rate < 0.15:
    num_obstacles += 1  # 增加障礙物數量

# 降級條件（降低難度）
elif avg_success_rate < 0.5 or avg_collision_rate > 0.3:
    num_obstacles -= 1  # 減少障礙物數量
```

### 難度等級

```python
min_difficulty = 3   # 3 個障礙物（起點）
max_difficulty = 10  # 10 個障礙物（上限）
```

---

## 🎯 三層架構總覽（層級式導航）

### 上層：AIT* 全域規劃器 (Global Planner)

**頻率**: 1 Hz ~ 5 Hz（低頻）

**輸入**:
- 地圖 (GridMap)
- 起點位置 (x, y)
- 終點位置 (x, y)

**輸出**:
- 全局路徑點列 (P_global)

### 中層：局部目標提取器 (Interface)

**頻率**: 10 Hz ~ 20 Hz（中頻）

**Carrot-on-Stick 策略**: 找到路徑上距離機器人前方 L 米的點作為局部目標

**坐標轉換**: World Frame → Robot Frame

### 下層：RL PPO 控制器 (Local Controller)

**頻率**: 50 Hz ~ 100 Hz（高頻）

**輸出**: [線速度, 角速度]

---

## 📈 訓練流程

### Phase 0-3 訓練階段（端到端 RL）

```
┌─────────────────────────────────────────────────────────┐
│  Phase 0 (基礎運動學)                                       │
│  ─────────────────────────────────                    │
│  障礙物：無                                              │
│  目標距離：3-8 米                                        │
│  觀測：無 LiDAR（最小觀測）                              │
│  訓練目標：學會控制輪子，理解相對座標                   │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  v1（Phase 1：靜態避障入門）                               │
│  ─────────────────────────────────                    │
│  障礙物：3 個靜態障礙物                                 │
│  目標距離：3-8 米                                         │
│  訓練目標：學會繞過簡單障礙物                             │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  v2（Phase 2：複雜靜態避障）                               │
│  ─────────────────────────────────                    │
│  障礙物：5 個靜態障礙物                                 │
│  目標距離：3-8 米                                         │
│  訓練目標：處理複雜障礙物配置                               │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  v3（Phase 3：長程導航 + 自適應課程）                       │
│  ─────────────────────────────────                    │
│  障礙物：無（專注於目標導航）                               │
│  目標距離：2-15 米（根據成功率自動調整）                     │
│  自適應機制：成功率 >80% 升級，<50% 降級              │
│  訓練目標：建立強大的「導航意志」                           │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Phase 4（未來：動態環境）                                   │
│  ─────────────────────────────────                    │
│  障礙物：動態障礙物                                       │
│  目標距離：混合                                             │
│  訓練目標：結合導航意志與動態避障                           │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 參數調優建議

### 前瞻距離 (Lookahead Distance)

| 環境類型 | 推薦值 | 說明 |
|---------|-------|------|
| 開闊空間 | 3.0-5.0m | 可以看遠一點，提高效率 |
| 有障礙物 | 1.5-2.0m | 需要更靈活地反應 |
| 窄通道 | 1.0-1.5m | 精確跟隨要求高 |

### 自適應課程參數

| 參數 | 默認值 | 說明 |
|------|--------|------|
| difficulty_increase_threshold | 0.8 | 成功率 > 80% 時升級 |
| difficulty_decrease_threshold | 0.5 | 成功率 < 50% 時降級 |
| max_collision_rate | 0.3 | 最大可接受碰撞率 |
| window_size | 100 | 滑動窗口大小（episodes） |

---

## 🐛 常見問題

### Q: 訓練時出現蛇行行為（左右擺動）？

**A**: 這通常發生在 Phase 2（5 個障礙物）。解決方案：
1. 降低障礙物數量，回到 Phase 1 或 v1
2. 提高目標導向獎勵權重
3. 使用 Phase 3（v3）先建立導航基礎

### Q: Agent 翻車滑行到目標？

**A**: 使用姿態門控（Posture Gating）：
```python
# 在運動獎勵中加入姿態檢查
reward = reward * is_upright.float()  # 翻車則獎勵歸零
```

### Q: 訓練不收斂？

**A**: 檢查：
1. 觀測是否歸一化到 [0, 1]
2. PPO std 是否為 0（觀測方差問題）
3. 學檢查 tensorboard 是否有日誌

---

## 📚 參考資料

### 算法

- **AIT***: Gammell, J. D., et al. "Adaptively Informed Trees (AIT*): Fast Asymptotically Optimal Path Planning through Adaptive Heuristics." *2020 IEEE International Conference on Robotics and Automation (ICRA)*.

- **PPO**: Schulman, J., et al. "Proximal Policy Optimization Algorithms." *arXiv preprint arXiv:1707.06347*.

### IsaacLab

- [IsaacLab Documentation](https://isaac-sim.github.io/IsaacLab/)
- [RL Training Guide](https://isaac-sim.github.io/IsaacLab/main/source/overview/reinforcement-learning/rl_existing_scripts.html)

---

**最後更新**: 2026-02-05
